{
  "name": "@discovery-platform/domain-vacancies",
  "version": "0.1.0",
  "private": true,
  "description": "The first reference Discovery domain module (job vacancies) — proves @discovery-platform/core is a genuinely product-neutral engine, not shaped around any one business. Consumes @discovery-platform/core only; never any application database, React or Firebase. See docs/architecture.md at the repository root.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@discovery-platform/core": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.4",
    "cheerio": "^1.2.0",
    "typescript": "^7.0.2"
  }
}
