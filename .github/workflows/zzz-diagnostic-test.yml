name: Minimal dispatch test
on:
  workflow_dispatch:
    inputs:
      foo:
        required: true
        default: 'bar'
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello ${{ github.event.inputs.foo }}"
