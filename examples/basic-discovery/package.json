{
  "name": "discovery-core-basic-example",
  "version": "0.1.0",
  "private": true,
  "description": "A minimal, generic consumer of @discovery-platform/core — proof that the core can be used by a program that has no domain-specific knowledge at all: no accommodations, scoring, claims, a database, React or Firebase. Documentation/test fixture only, not a real product.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "crawl": "node run.mjs"
  },
  "dependencies": {
    "@discovery-platform/core": "^0.1.0"
  }
}
