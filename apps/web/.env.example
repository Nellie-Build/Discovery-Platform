# Copy to .env.local (gitignored) if you need to override the default. Vite only reads .env
# files from this directory (apps/web/), never the repository root's own .env.
VITE_API_URL=http://127.0.0.1:3000/api/v1
